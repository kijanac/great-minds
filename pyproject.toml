[project]
name = "great-minds"
version = "0.1.0"
description = "Corpus of collected works stored as markdown"
requires-python = ">=3.13"
dependencies = [
    "crawl4ai>=0.8.0",
    "openai>=1.0.0",
    "ruamel.yaml>=0.18.0",
]
