{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop shell capability set for auth secrets, folder ingestion, and optional local backend discovery.",
  "windows": [
    "main"
  ],
  "permissions": [
    "auth-secrets",
    "dialog:default"
  ]
}
