{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop shell capability set. The thin wrapper exposes no native IPC permissions yet.",
  "windows": [
    "main"
  ],
  "permissions": [
    "auth-secrets"
  ]
}
