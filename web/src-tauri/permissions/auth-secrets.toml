[[permission]]
identifier = "auth-secrets"
description = "Allow the main window to store, read, and delete the Great Minds refresh token in the OS credential store."
commands.allow = [
  "store_refresh_token",
  "get_refresh_token",
  "delete_refresh_token",
]
