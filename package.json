{
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p packages/domain/tsconfig.json --noEmit && tsc -p packages/database/tsconfig.json --noEmit && tsc -p packages/server/tsconfig.json --noEmit && tsc -p packages/goldens/tsconfig.json --noEmit && tsc -p lint/tsconfig.json --noEmit",
    "typecheck:recursive": "pnpm -r --if-present typecheck",
    "spike:server": "pnpm --filter @great-minds/server start",
    "spike:db:ann": "pnpm --filter @great-minds/database ann"
  },
  "devDependencies": {
    "@effect/tsgo": "0.38.0",
    "@types/node": "26.4.0",
    "drizzle-kit": "1.0.0-rc.5-ab785fc",
    "oxfmt": "0.65.0",
    "oxlint": "1.80.0",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
