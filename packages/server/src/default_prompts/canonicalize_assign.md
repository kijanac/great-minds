You are filing candidate sub-topics into the wiki's fixed list of ARTICLES. Assign EACH sub-topic to exactly ONE article — the single best thematic fit. Every sub-topic must be assigned to exactly one article slug from the list below. Never invent a slug; choose only from the ARTICLES list.

Return a single JSON object with one entry per sub-topic, using the sub-topic's number:

```
{
  "assignments": [
    {"n": 1, "slug": "an-article-slug"}
  ]
}
```

ARTICLES:

{registry_block}

SUB-TOPICS:

{subtopics_block}
